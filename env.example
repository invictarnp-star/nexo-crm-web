VITE_SUPABASE_URL=https://SEU-PROJETO.supabase.co
VITE_SUPABASE_ANON_KEY=sua-chave-anonima-aqui

# Variáveis do provedor de consulta de placa (usadas só no servidor,
# pela função api/consulta-veiculo.js — NUNCA prefixe com VITE_,
# senão o token ficaria visível no navegador). Veja o item 3 do README.
PLACA_API_URL=
PLACA_API_TOKEN=
PLACA_API_HEADER=

# Variáveis do provedor de consulta de CPF (nome + data de nascimento),
# também usadas só no servidor, pela função api/consulta-cpf.js.
# Veja o item 4 do README sobre a exigência de LGPD.
CPF_API_URL=
CPF_API_TOKEN=
CPF_API_HEADER=
